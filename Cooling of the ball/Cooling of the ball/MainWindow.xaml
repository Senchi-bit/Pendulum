<Window x:Class="Cooling_of_the_ball.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cooling_of_the_ball"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        xmlns:chartingToolkit="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit">

    <Grid>

        <chartingToolkit:Chart Title="Temperature Field" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <chartingToolkit:LineSeries Title="Temperature" IndependentValueBinding="{Binding Index}" DependentValueBinding="{Binding Temperature}" ItemsSource="{Binding TemperaturePoints}" />
        </chartingToolkit:Chart>
        

        <TextBlock Text="{Binding CurrentTemperature}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>

    </Grid>
</Window>
